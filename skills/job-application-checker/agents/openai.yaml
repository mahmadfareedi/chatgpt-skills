interface:
  display_name: Job Application Checker
  short_description: Resolves the job post, checks Sent mail first, then writes an email only when safe.
policy:
  products:
  - chatgpt
  - codex
  - api
  - atlas
  allow_implicit_invocation: true
